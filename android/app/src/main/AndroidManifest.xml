<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 필수 및 위치/저장소 권한 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <application
        android:label="yamyam_road"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- ⬇️ Google AdMob 앱 ID 메타데이터 (env로 관리) ⬇️ -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${ADMOB_APP_ID}"/>
        <!-- ⬆️ 추가 완료 ⬆️ -->

        <!-- 구글 지도 API Key 설정 (.env의 GOOGLE_MAPS_API_KEY를 build.gradle.kts를 통해 전달받음) -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- 카카오 로그인 AppKey (env로 관리) -->
        <meta-data android:name="com.kakao.sdk.AppKey" android:value="${KAKAO_APP_KEY}" />

        <!-- 카카오 로그인 인증 Activity -->
        <activity
            android:name="com.kakao.sdk.flutter.AuthCodeCustomTabsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- scheme 자체에 앱키가 포함되므로 KAKAO_SCHEME으로 별도 관리 -->
                <data android:host="oauth" android:scheme="${KAKAO_SCHEME}" />
            </intent-filter>
        </activity>

        <!-- 네이버 로그인 SDK 설정 (env로 관리) -->
        <meta-data
            android:name="com.naver.sdk.clientId"
            android:value="${NAVER_CLIENT_ID}" />
        <meta-data
            android:name="com.naver.sdk.clientSecret"
            android:value="${NAVER_CLIENT_SECRET}" />
        <meta-data
            android:name="com.naver.sdk.clientName"
            android:value="${NAVER_CLIENT_NAME}" />
    </application>

    <!-- 외부 앱 및 브라우저 실행 허용 설정 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- 네이버 지도 패키지 및 딥링크 스키마 -->
        <package android:name="com.nhn.android.nmap" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="nmap" />
        </intent>

        <!-- 카카오맵 패키지 및 딥링크 스키마 -->
        <package android:name="net.daum.android.map" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="kakaomap" />
        </intent>

        <!-- 웹 브라우저(Fallback) 연결용 HTTP/HTTPS -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>